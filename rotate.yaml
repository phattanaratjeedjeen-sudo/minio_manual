keyrotate:
  apiVersion: v1
  bucket: a-buck
  prefix: ""          
  encryption:
    key: key3
  flags:
    filter:
      plaintextOnly: false
      # kmskey: "key-v1"
    retry:
      attempts: 5
      delay: "500ms"
